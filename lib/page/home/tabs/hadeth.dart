import 'package:flutter/material.dart';
class HadithTabs extends StatelessWidget {
  const HadithTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Hadith Tabs"));
  }
}
