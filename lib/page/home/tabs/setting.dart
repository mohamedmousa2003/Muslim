import 'package:flutter/material.dart';

class SettingTabs extends StatelessWidget {
  const SettingTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Quran Tabs"));
  }
}
